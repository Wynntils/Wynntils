Guild Map
- Added button to display abbreviated territory names instead of guild tag

Misc Item Guide
- Added broken dungeon keys

Fixes
- Initial download of data will now use selected source instead of always using the CDN
- Held Item Name overlay will now display when items are renamed
- Newly captured territories will use resource colors in hybrid mode when resource colors enabled
- Fixed `with_player_sprite_font` crashing with invalid UUIDs
- Fixed `wynntils_role` crashing for users without a role
